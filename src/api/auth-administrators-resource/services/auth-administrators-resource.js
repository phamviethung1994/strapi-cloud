'use strict';

/**
 * auth-administrators-resource service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::auth-administrators-resource.auth-administrators-resource');
