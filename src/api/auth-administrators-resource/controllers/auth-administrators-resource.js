'use strict';

/**
 * auth-administrators-resource controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::auth-administrators-resource.auth-administrators-resource');
