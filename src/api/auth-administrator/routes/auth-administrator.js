'use strict';

/**
 * auth-administrator router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::auth-administrator.auth-administrator');
