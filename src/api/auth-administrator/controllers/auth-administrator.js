'use strict';

/**
 * auth-administrator controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::auth-administrator.auth-administrator');
